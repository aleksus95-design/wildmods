# wildmods

SCUM modding workspace artifacts and local rebuild scripts.

## Loading key art

The loading screen replacement is kept as a local rebuild workflow in
[`loading-keyart/`](loading-keyart/) instead of committing extracted
`.uasset`, `.uexp`, or `.pak` files into the public repository.

Current local output path after the latest build:

```text
C:\Users\aleks\Desktop\Новая папка (2)\zzzz_SCUM_LoadingKeyArt_WILDA_4K_P.pak
```

Pak SHA1:

```text
01E7292EBA6107D9C63F2163417999DB7F65541C
```
